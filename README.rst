Luke Pomfrey's dotfiles
=======================

Just my dotfiles. Config is in ``dotfiles.conf``
with installation and dependency handling in the ``fabfile.py``.

Requires ``python``, ``fabric``, and ``git``.
