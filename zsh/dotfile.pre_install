#!/bin/sh

aptitude search "~izsh" || aptitude install zsh

if [ -d ~/.oh-my-zsh ]; then
    cd ~/.oh-my-zsh
    git pull
else
    cd ~
    git clone git://github.com/robbyrussell/oh-my-zsh.git ~/.oh-my-zsh
fi
